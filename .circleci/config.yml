version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: interactive_computer_graphics
          command: echo Will get this working.
